[project]
name = "databricks-claude-starter"
version = "0.1.0"
description = "Databricks + Claude Code Starter Kit - Write code, manage workspace, and query data locally"
requires-python = ">=3.12"
dependencies = [
    "databricks-connect==17.3.*",
]
readme = "README.md"

[tool.uv]
package = false
